name: Java 11 CI/CD with Docker (Versioned Tags)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup JDK 11
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'  # Bisa juga 'adopt', 'zulu', dll.
          java-version: '11'

      - name: Extract Version from pom.xml
        id: extract_version
        run: echo "VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)" >> $GITHUB_ENV

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Pull, latest tag and Push Docker Image
        run: |
          docker pull ${{ secrets.DOCKER_USERNAME }}/authorization-server:${{ env.VERSION }} 
          docker tag ${{ secrets.DOCKER_USERNAME }}/authorization-server:${{ env.VERSION }} ${{ secrets.DOCKER_USERNAME }}/authorization-server:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/authorization-server:latest
